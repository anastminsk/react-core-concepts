import React from 'react';

function HelloReact() {
    return (
        <h2 className='hello' style={{color: 'green'}}>
            Hello React!
        </h2>
    );
}

// let HelloReact = () => <h2 className='hello' style={{color: 'green'}}>Hello React!</h2>;

export default HelloReact;
